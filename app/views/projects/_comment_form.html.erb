<%= form_with(model: comment, url: url, local: false) do |form| %>
  <div class'form-group'>
   <div class="row">
  <div class="col-sm-4"><%= form.text_field :content, placeholder: "add comment", required: true, class: 'form-control'%></div>
  <div class="col-sm-4"><%= form.submit  :class =>'btn'%></div>
</div>

  
  </div>  

  
<% end %>
